 <%= simple_form_for([@category, @article]) do |f| %>
    <%= f.input :title %>
    <%= f.input :meta_title %>
    <%= f.input :photo, as: :file %>
    <%= f.input :summary %>
    <%= f.input :content %>
    <%= f.input :slug %>
    <%= f.submit "Add Article" %>
  <% end %>
