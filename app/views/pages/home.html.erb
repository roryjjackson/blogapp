<div class="page-content">
  <div class="home-page-new-map-buttons">
    <%= link_to "New post", new_post_path, class: "btn btn-ghost" if policy(Post).new? %>
    <%= link_to "View Map", posts_path, class: "btn btn-ghost" %>
  </div>
  <div class="home-header">
    <h1>Featured</h1>
  </div>
  <div class="posts-container">
    <div class="posts-grid">
      <% @posts.each do |post| %>
        <%= link_to post do %>
          <div class="post-card">
            <%= cl_image_tag post.photo.key %>
            <div class="post-card-text">
              <h2><%= post.title %></h2>
              <p>Created by - <%= post.user_id %></p>
              <p><%= post.created_at.to_date %></p>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
