<p>article show page</p>
<% @article.photos.each do |photo| %>
  <%= cl_image_tag photo.key, height: 300, width: 400, crop: :fill %>
<% end %><%= @article.title %> |
<%= @article.meta_title %> |
<%= @article.summary %>  |
<%= @article.content %>  |
<%= @article.slug %>  |
<%= @article.user_id %>
<%= link_to "new comment", new_category_article_comment_path(@article.category_id, @article) %>
<% @comments.each do |comment| %>
  <%= comment.title %>
  <%= comment.content %>
  <%= link_to "edit this comment", edit_category_article_comment_path(@article.category_id, @article, comment) %>
  <%= link_to "show this specific comment", category_article_comment_path(@article.category_id, @article, comment) %>
  <%= link_to "destroy this comment", category_article_comment_path(@article.category_id, @article, comment), data: { turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
  <%# <%= link_to "edit this comment", edit_article_comment_path(comment) %> %>
<% end %>

<%# <%= link_to "edit", edit_article_path(article) %>
<%# <%= link_to "destroy", article_path(article), data: { turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
<%# <%= link_to "show comments", article_comments_path(@article) %> %>
<%= link_to  "back to category show", category_path(@article.category_id) %>
<div style="width: 100%; height: 600px;"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
