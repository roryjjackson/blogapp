 <%= simple_form_for (@category) do |f| %>
    <%= f.input :title %>
    <%= f.input :meta_title %>
    <%= f.input :content %>
    <%= f.input :slug %>
    <%= f.input :photo, as: :file %>
    <%= f.submit "Add category" %>
  <% end %>
